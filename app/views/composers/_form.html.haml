= form_for(@composer) do |f|
  - if @composer.errors.any?
    .alert.alert-block.alert-error
      <button type="button" class="close" data-dismiss="alert">×</button>
      %h4
        = pluralize(@composer.errors.count, "error")
        prohibited this composer from being saved:
      %ul
        - @composer.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :full_name
    = f.text_field :full_name

  .field
    = f.label :english_name
    = f.text_field :english_name
    %span.help-block
      If different from the full name, e.g. George Frideric Handel for Georg
      Friedrich Händel.

  .field
    = f.label :short_name
    = f.text_field :short_name
    %span.help-block
      A short moniker for this composer, the name this composer is usually
      called. Mozart for Wolfgang Amadeus Mozart, Bach for Johann Sebastian
      Bach.

  .field
    = f.label :description
    = f.text_area :description, :size => "120x12"

  .field
    = f.label :date_born
    = f.text_field :date_born

  .field
    = f.label :date_died
    = f.text_field :date_died

  .actions
    = f.submit :class => 'btn'
